(do 
 (set 'add (lambda (x y) (+ x y))) 
 (do (set 'f (lambda (n) 
	       (if (= n 0) 
		   (throw 42) 
		 (add (f (- n 1)) 1)))) 
     (add (try (lambda () 
	    (f 300)) 
      (lambda (v cc) (cc v)))  1)))


;; (defun add (x y)
;;   (+ x y))

;; (defun f (n)
;;   (if (= n 0)
;;       42
;;       (add (f (- n 1)) 1)))

;; (f 300)
