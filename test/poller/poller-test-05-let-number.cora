;; Poller 测试 05: let 绑定普通数字
;; 预期结果: ✅ 应该成功
;; 状态: ✅ 通过 - 验证 let 本身没有问题

(import "cora/lib/io")
(import "cora/lib/parallel/internal")

(display "=== 测试 05: let 绑定普通数字 ===\n")

(let x 42
  (begin
    (display "x=")
    (display x)
    (display "\n")
    (display "测试通过!\n")
    x))
